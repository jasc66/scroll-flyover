# Copy this file to .env and fill in a real value. .env is gitignored — never commit it.

# npm "Automation" or "Publish" token (from https://www.npmjs.com/settings/<user>/tokens),
# used by .npmrc to authenticate `npm publish`.
NPM_TOKEN=
